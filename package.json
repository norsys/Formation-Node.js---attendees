{
  "name": "stock-management",
  "version": "1.0.0",
  "author": "",
  "license": "MIT",
  "workspaces": [
    "domain",
    "infrastructure/*",
    "interfaces/*"
  ],
  "scripts": {
    "start:cli": "npm start -w interfaces/cli",
    "start:api": "npm start -w interfaces/api",
    "start:api-dev": "npm run start:dev -w interfaces/api",
    "test:domain": "npm test -w domain",
    "test": "npm test --workspaces --if-present"
  },
  "dependencies": {
    "@nestjs/config": "^12.0.0"
  }
}
